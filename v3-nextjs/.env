# SQLite datasource for the v3 likes backend. Not a secret — it's a local file path,
# the same value CI injects — committed so `next build`/`next start` and `prisma migrate`
# work on a fresh clone with zero setup. Real secrets belong in .env.local (still ignored).
DATABASE_URL="file:./dev.db"
